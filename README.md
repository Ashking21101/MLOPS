```bash
git status
git add . (or git add filename)
git commit -m "message"
git pust origin main

git pull

in case of bug go to commit button o get previous version of code



------------ 

create new branch
git checkout -b newbranchname
git add .
git commit -m "message"
to save use git pust origin newbranchname
--------

to swith between branches 
git checkout branchname